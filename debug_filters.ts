import { buildPricingFilters } from './server.ts'; // wait server.ts does not export buildPricingFilters

// I will just read server.ts and eval it, or better, modify server.ts temporarily to console.log it.
